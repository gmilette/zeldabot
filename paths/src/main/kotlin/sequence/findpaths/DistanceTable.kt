package sequence.findpaths

import bot.state.MapLoc

class DistanceTable {
    fun dist(from: Destination, to: Destination): Int {
        return 1
    }

    fun dist(from: MapLoc, to: MapLoc): Int {
        return 1
    }
}